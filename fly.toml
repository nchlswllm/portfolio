app = "nchlswllm-portfolio"
primary_region = "ord"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_start_machines = true
  auto_stop_machines = "suspend"
  min_machines_running = 0

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
